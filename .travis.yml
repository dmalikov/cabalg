language: haskell

ghc:
 - 7.2
 - 7.4
 - 7.6
 - 7.8

before_install:
    - ghc --version
    - cabal configure --enable-tests

install:
    - ghc --version
    - cabal build
    - cabal test

script:
    - script/sdist-unpack-install.sh
